\bookbackmattersection{Afterword}

The package is designed for XeLaTeX and assumes the repository root as the
working directory so that bundled fonts in \texttt{assets/fonts} are found
reliably during compilation.
